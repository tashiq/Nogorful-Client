import React from 'react';

const Nav = () => {
    const navItems = document.querySelectorAll('.nav-item');
    console.log(navItems);
    return (
        <div>

        </div>
    );
};

export default Nav;